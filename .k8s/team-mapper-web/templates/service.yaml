apiVersion: v1
kind: Service
metadata:
  name: {{ include "team-mapper-web.fullname" . }}
  labels:
    {{- include "team-mapper-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "team-mapper-web.selectorLabels" . | nindent 4 }}
