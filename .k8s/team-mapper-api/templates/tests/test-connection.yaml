apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "team-mapper-api.fullname" . }}-test-connection"
  labels:
    {{- include "team-mapper-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "team-mapper-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
